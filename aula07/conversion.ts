let variavel: string = '10'

let novaVariavel = Number(variavel)
console.log(`novaVariavel possui tipo ${typeof(novaVariavel)} e valor ${novaVariavel}`)

// NAO DÁ PRA TROCAR O TIPO, DEPOIS DE TIPADA. MAS PODEMOS CRIAR UMA NOVA VARIÁVEL , TIPANDO DIFERENTEMENTE DA "ORIGINAL"
// E USANDO MESMOS DADOS.